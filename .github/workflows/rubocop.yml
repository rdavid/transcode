# SPDX-FileCopyrightText: 2025-2026 David Rabkin
# SPDX-License-Identifier: 0BSD
#
# The pull-requests:write permission below is scoped to the rubocop job
# alone (not the workflow default) so that reviewdog can post inline
# review comments on the triggering pull request.
---
name: rubocop
'on':
  pull_request:
    paths: &paths
      - '**/*.rb'
      - bin/**
      - .rubocop.yml
      - Gemfile
      - Gemfile.lock
      - Rakefile
      - '*.gemspec'
      - .github/workflows/rubocop.yml
  push:
    branches: [master]
    paths: *paths
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
jobs:
  rubocop:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write
    env:
      BUNDLE_ONLY: rubocop
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: ruby/setup-ruby@v1.321.0
        with:
          ruby-version: 3.2
          bundler-cache: true
      - uses: reviewdog/action-rubocop@v2.22.0
        with:
          reporter: github-pr-review  # Default is github-pr-check
          skip_install: true
          use_bundler: true
